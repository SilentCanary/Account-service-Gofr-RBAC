services:
  db:
    image: postgres
    restart: always
    environment:
        POSTGRES_DB: auth_service
        POSTGRES_USER: admin
        POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"
    volumes:
      - ./sql/schema.sql:/docker-entrypoint-initdb.d/schema.sql